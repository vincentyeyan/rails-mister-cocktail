<h1 class="text-center">Cocktails</h1>


<%= link_to 'New Cocktail', new_cocktail_path, class: "btn btn-primary btn-lg" %>

<div>
  <% @cocktails.each do |cock| %>
  <%= link_to cocktail_path(cock) do %>
  <%= cock.name %>
  <%= link_to "delete", cocktail_path(cock), method: :delete, data: { confirm: "Are you sure?"} %>
</div>
  <% end %>
  <% end %>
